# 考研打卡系统 · 使用说明

一个跑在自己设备上的打卡网页应用，无需注册、无需安装 App，数据完全自己掌控。

## 一、手机上怎么用（安卓 · 独立安装，不依赖电脑）

1. 安卓 Chrome 打开：**https://xjb1337.github.io/kaoyan-checkin/**
2. 点右上角 **⋮ 菜单 → 添加到主屏幕**（出现「安装应用」就选它）；
3. 桌面上出现「考研打卡」图标，以后每天点它打卡。

- 第一次打开成功后，应用已缓存进手机，**以后没网也能打卡**；
- 数据保存在手机 Chrome 里，与电脑无关；换手机时用「设置 → 备份与恢复」导出/导入 JSON；
- 源码公开在 `github.com/xjb1337/kaoyan-checkin`（仅应用代码，不含任何个人数据），想删除时进仓库 Settings → Danger Zone → Delete。

### 备用方式（局域网，可选）

家里 Mac 上还跑着一个可选的同步服务，作为 GitHub 访问不畅时的备用入口：同一 Wi-Fi 下访问 `http://192.168.1.130:8765`（IP 可能随网络变化，Mac 上用 `ipconfig getifaddr en0` 查当前值；数据与在线版相互独立）。不需要的话删除：
`launchctl bootout gui/$(id -u)/com.liuyuhang.kaoyan-checkin`，再删 `~/Library/LaunchAgents/com.liuyuhang.kaoyan-checkin.plist` 和 `考研打卡系统` 文件夹。

## 二、它有什么功能

### 今日
- **任务列表**按日期自动匹配当前备考阶段（阶段0a 生理启动 → 0b 生化 → 1 基础期 → 2 强化 → 3 冲刺 → 4 终极 → 复试期），每天不同；
- **最低完成线**：背单词 50 词 + 西综刷题 10 题。状态再差完成这两项就算打卡；
- **日期选择器**：直接点日期跳到任意一天补卡（不用连点箭头）；
- **撤销**：误点后底部会弹出「已打卡 · 撤销」，4 秒内可撤回；
- **学习时长与科目**：点 ±0.5h 记总时长；点「+0.5 西综/英语/政治」记录科目时间（统计页看时间分配）；
- **三问复盘**：收获 / 卡住的地方 / 明天第一件事，30 秒填完；
- **手势**：任务行**左滑或右滑**可快速切换完成；自定义任务**长按**可删除。

### 进度（新增，最重要）
- **计划 vs 实际**：六科进度条，自动算出"计划应完成 N 章 / 你已完成 M 章"，并标出**领先 / 落后 / 正常**；
- **章节清单**：生理 12 章、生化 12 章、病理 13 章、内科 8 章、外科 9 章，学完一章点一下；
- **模考成绩**：录入每次套卷分数，生成**趋势折线图** + **200 分决策线**，自动判定"可冲麻醉/外科"或"建议调整志愿档位"。

### 周目标
- 3 个必须完成 + 弹性任务 + 周日复盘；
- **自动周报**：自动汇总本周打卡天数、时长（含目标对比）、周目标完成度、章节进度，一键复制成文字。

### 统计
- **本周完成率**（主指标，配合你的周计划制）、连续打卡、累计时长、补签卡余额、距初试天数；
- **科目时间分布**、**近 8 周时长柱状图**（橙色=未达日目标）；
- **热力图**：全程打卡热度，带月份标签与图例。

### 节点
- 备考全程进度条 + **14 个关键节点倒计时**（含 3 个决策检查点、预报名、报名截止、网上确认、准考证、初试、出分、复试）；
- **日历导出（强烈建议用一次）**：点「📅 导出日历文件」，导入手机日历后，会得到**每天 19:00 打卡提醒** + 全部关键节点提醒（节点提前 1 天预警）。这是唯一能让应用主动提醒你的方式。

### 补签卡（针对状态波动）
- 每月 2 张。某天漏了最低线，切到那天点「🩹 用补签卡」即可保住连胜，不必因为断一天而放弃。

### 设置
- 初试日期、固定每日任务、补签卡余额、**备份与恢复**（导出/导入 JSON）、数据健康检查。

## 三、数据存哪、怎么同步

- 数据保存在**手机 Chrome 本机**（localStorage），与电脑无关；第一次打开成功后断网也能用；
- **备份提醒**：超过 30 天没备份，首页会出现提醒横幅。清浏览器数据前务必先在「设置」导出备份；
- 换手机：旧机「导出备份（下载 JSON）」→ 新机「导入并合并」；
- 家里 Mac 上有一个可选的局域网同步服务（数据按天合并），作为 GitHub 访问不畅时的备用入口。

## 四、日常维护

| 情况 | 处理 |
|---|---|
| 手机打不开页面 | 确认 Mac 开机且没睡眠、和手机同一 Wi-Fi；首次连接时若 Mac 弹出防火墙询问，点"允许" |
| Mac 的局域网 IP 变了 | 用 `http://Mate-Book-X.local:8765`，或到 Mac 上重新查 IP：终端输入 `ipconfig getifaddr en0` |
| 服务没在跑 | 终端执行：`launchctl kickstart -k gui/$(id -u)/com.liuyuhang.kaoyan-checkin` |
| 手动启动（不用 LaunchAgent） | `PORT=8765 python3 /Users/liuyuhang/ZCodeProject/考研打卡系统/server.py` |
| 彻底删除本系统 | `launchctl bootout gui/$(id -u)/com.liuyuhang.kaoyan-checkin`，再删 `~/Library/LaunchAgents/com.liuyuhang.kaoyan-checkin.plist` 和 `考研打卡系统` 文件夹 |

## 五、文件清单

```
考研打卡系统/
├── index.html            应用本体（单文件网页，可离线拷走用）
├── server.py             局域网同步 + 静态服务（Python 标准库，无依赖）
├── sw.js                 Service Worker（HTTPS 环境下提供离线缓存）
├── manifest.webmanifest  PWA 清单
├── icon-180.png / icon-512.png  应用图标
└── state.json            打卡数据（服务自动生成）
```

> 说明：该网页在局域网 IP（http）下浏览器不启用离线缓存，属于正常现象；若以后想要"随时随地访问 + 离线可用"，可把整个文件夹免费部署到 Cloudflare Pages / GitHub Pages（需注册账号），应用本身无需任何修改。
